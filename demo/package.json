{
  "name": "demo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@examples/import-vite-build": "workspace:*",
    "@examples/single-vite-config": "workspace:*"
  }
}